# Copy to Api/.env for local overrides (gitignored). All have sane defaults, so
# the BFF runs without a .env when the values below are passed via docker-compose.
PAPR_JWT_SECRET=dev-insecure-secret-change-me
AGENT_URL=http://agent:2024
PAPR_WEB_ORIGINS=http://localhost:5173,http://localhost:5174
