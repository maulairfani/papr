# Copy to .env and fill in. Loaded by `langgraph dev` and docker-compose.

# Model in provider:model format (see deepagents supported models).
PAPR_MODEL=openai:gpt-5.4-nano

# Required for the default OpenAI model.
OPENAI_API_KEY=sk-proj-...
